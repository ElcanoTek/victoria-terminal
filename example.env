# Victoria Terminal Environment Variables
# Copy this file to .env and fill in your actual API keys

# OpenRouter API Key for AI model access
# Get your API key from: https://openrouter.ai
OPENROUTER_API_KEY="sk-or-your-api-key-here"

# Gamma API Key for presentation generation
# Get your API key from: https://gamma.app (requires Pro account or higher)
GAMMA_API_KEY="sk-gamma-your-api-key-here"

# SendGrid API Key for transactional email support via the SendGrid MCP server
# Generate this in your SendGrid dashboard under Email API > Integration Guide
SENDGRID_API_KEY="SG.your-sendgrid-api-key-here"

# Default "from" address for SendGrid emails
SENDGRID_FROM_EMAIL="victoria@elcanotek.com"

# Browserbase API Key for remote browser sessions via the Browserbase MCP server
# Get your API key from: https://www.browserbase.com (requires an account)
BROWSERBASE_API_KEY="bb-your-browserbase-api-key-here"

# Browserbase Project ID (required for browser session management)
# Find this in your Browserbase dashboard under Project Settings
BROWSERBASE_PROJECT_ID="your-browserbase-project-id-here"

# Gemini API Key for Browserbase MCP (used for AI-powered browser interactions)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# -------------------------------
# BrowserOS MCP Configuration
# Connects Victoria Terminal to BrowserOS for local browser automation
# -------------------------------

# BrowserOS MCP URL (leave empty or remove to disable)
# To find your URL:
#   1. Open BrowserOS and go to chrome://browseros/settings
#   2. Click on "MCP" in the sidebar
#   3. Copy the MCP URL shown (default port is 9100)
#
# ⚠️  PORT STABILITY NOTE:
# BrowserOS uses dynamic port allocation. If port 9100 is busy at startup,
# it increments (9101, 9102, etc.) and saves the new port to preferences.
# This can cause the URL below to become stale after restarts.
#
# If you experience connection issues:
#   - Check the current port in chrome://browseros/settings → MCP
#   - Update the port number below to match
#   - Or use our helper script to restart BrowserOS on a fixed port:
#     curl -fsSL https://raw.githubusercontent.com/ElcanoTek/victoria-terminal/main/scripts/restart_browseros.sh | bash -s -- --install
#     Then run: restart-browseros
#
# See: https://github.com/browseros-ai/BrowserOS/issues/199
#
# Note: Use "host.containers.internal" instead of "127.0.0.1" when running in a container
BROWSEROS_URL="http://host.containers.internal:9100/mcp"

# -------------------------------
# Snowflake MCP Configuration
# Use your Programmatic Access Token (PAT) as SNOWFLAKE_PASSWORD
# -------------------------------
SNOWFLAKE_ACCOUNT="your-snowflake-account-here"       # e.g. xy12345.us-east-1
SNOWFLAKE_USER="your-snowflake-username-here"         # e.g. AIUSER
SNOWFLAKE_PASSWORD="your-pat-token-here"              # paste your PAT (API key) here

# -------------------------------
# AWS SES + S3 Email Inbox Configuration
# For receiving emails via AWS SES and storing in S3
# -------------------------------

# AWS Credentials (create an IAM user with S3 read access)
AWS_ACCESS_KEY_ID="your-aws-access-key-id-here"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key-here"
AWS_REGION="us-east-2"

# S3 bucket where SES deposits emails
EMAIL_S3_BUCKET="victoria-email-inbox"

# Prefix (folder) within the bucket where emails are stored
EMAIL_S3_PREFIX="emails/"

# Local directory to save downloaded attachments
EMAIL_ATTACHMENT_DIR="${VICTORIA_HOME}/email_attachments"

# File to track when inbox was last checked
EMAIL_LAST_CHECK_FILE="${VICTORIA_HOME}/email_last_checked.txt"

# -------------------------------
# All-Time Quarterback Orchestration Configuration
# For fleet management and remote task execution
# See: https://github.com/ElcanoTek/all-time-quarterback
# -------------------------------

# NOTE: These variables are typically injected by the remote-runner when launching
# containers, but can be set manually for standalone testing.

# Orchestrator URL
# This enables the Status Reporter MCP server for task status updates
# ORCHESTRATOR_URL="http://quarterback.example.com:8000"

# Node API Key
# Authenticates the container with the orchestrator for status reporting
# NODE_API_KEY="your-node-api-key-here"
